{% extends 'base.html.twig' %}

{% set page_title = 'New document' %}
{% set page_header = true %}

{% block body %}
<div class="py-8">
    <div class="flex items-center gap-3 mb-6">
        <a href="{{ path('ledger_index') }}"
            class="text-muted-foreground hover:text-foreground transition-colors">
            <twig:ux:icon name="material-symbols:arrow-back" class="size-5" />
        </a>
        <h1 class="font-heading font-bold text-3xl text-foreground">Register new document number</h1>
    </div>

    <twig:DocumentWizard />
</div>
{% endblock %}
